# resume
Jakes Resume

## Testing

To run the tests, you need to install the dependencies and Playwright browsers:

```bash
pip install pytest pytest-playwright
playwright install chromium
```

Then run the tests with:

```bash
pytest
```
